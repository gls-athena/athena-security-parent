package com.gls.athena.security.iam.boot;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Athena Security IAM 启动类
 *
 * @author athena 自动生成
 * @version 0.0.1-SNAPSHOT
 */
@SpringBootApplication
public class IamBootApplication {

    /**
     * Athena Security IAM 启动入口
     *
     * @param args 参数
     */
    public static void main(String[] args) {
        SpringApplication.run(IamBootApplication.class, args);
    }
}